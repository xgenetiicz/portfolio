// TODO: replace with the real login form (email/password + OTP step) once we build this
export default function LoginPage() {
  return (
    <main className="flex min-h-[60vh] items-center justify-center px-6 font-mono text-text">
      <p className="text-muted">Login page — coming soon.</p>
    </main>
  );
}